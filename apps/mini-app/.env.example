# API base (default works with `pnpm --filter @workspace/api dev`)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Generate: pnpm --filter @workspace/db dev:initdata <chatId> [firstName] [lastName]
# (script rewrites this file automatically)
NEXT_PUBLIC_DEV_INIT_DATA=
